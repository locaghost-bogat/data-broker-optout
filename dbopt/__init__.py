"""
data-broker-optout
==================

A DeleteMe-style privacy tool for macOS 10.10+ that prepares, submits guidance
for, and tracks personal-data deletion / opt-out requests to data broker sites.

The package is pure Python standard library (Tkinter GUI + CLI) so it runs on any
macOS with a python.org Python 3 install and no third-party dependencies.
"""

__version__ = "1.0.0"
__app_name__ = "Data Broker Opt-Out"
__bundle_id__ = "com.local.databrokeroptout"
